import { OnModelChangeDirective } from './on-model-change.directive';

describe('OnModelChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new OnModelChangeDirective();
    expect(directive).toBeTruthy();
  });
});
