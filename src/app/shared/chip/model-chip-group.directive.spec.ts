import { ModelChipGroupDirective } from './model-chip-group.directive';

describe('ModelChipGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new ModelChipGroupDirective();
    expect(directive).toBeTruthy();
  });
});
