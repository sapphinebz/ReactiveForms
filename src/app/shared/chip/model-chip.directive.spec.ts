import { ModelChipDirective } from './model-chip.directive';

describe('ModelChipDirective', () => {
  it('should create an instance', () => {
    const directive = new ModelChipDirective();
    expect(directive).toBeTruthy();
  });
});
