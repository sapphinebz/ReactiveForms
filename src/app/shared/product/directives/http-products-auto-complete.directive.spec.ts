import { HttpProductsAutoCompleteDirective } from './http-products-auto-complete.directive';

describe('HttpProductsAutoCompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new HttpProductsAutoCompleteDirective();
    expect(directive).toBeTruthy();
  });
});
